# E2 — Add sphere cap built-in mesh generator to benchmark

## GitHub Issue
https://github.com/educelab/OpenABF/issues/48

## Summary
Add a built-in sphere cap mesh generator to the flattening benchmark example so users can benchmark parameterization on a curved surface without needing an external mesh file.

## Acceptance Criteria
- [ ] Sphere cap mesh generator added to benchmark
- [ ] Generator accepts configurable resolution parameters
- [ ] Benchmark can run without external mesh files using the built-in generator
