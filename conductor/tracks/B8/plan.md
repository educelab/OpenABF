# B8 Implementation Plan

Branch: `b8-lscm-area-rescale`.

TDD-strict (per `conductor/workflow.md`): tests are written and confirmed Red
before implementation. Manual verification checkpoint after each phase.

## Phase 1: Tests (Red)
- [ ] 1.1 `detail::lscm` unit tests for the rescale math: given known per-face
  3D and UV areas, assert `GlobalAreaMin` produces the area-weighted-median
  factor and `LowStretchReference` produces the least-distorted face's factor.
- [ ] 1.2 ABLSCM area-preservation test: build a mesh whose auto-selected
  boundary edge is deliberately stretched; assert `GlobalAreaMin` gives total UV
  area ≈ total 3D area within tolerance and beats `Off`; `LowStretchReference`
  also beats `Off`.
- [ ] 1.3 ABLSCM regression test: `Off` reproduces the current output exactly.
- [ ] 1.4 ABLSCM instance-form test: `set_rescale_mode(mode) + compute()` equals
  the static `Compute(mesh, mode)` overload.
- [ ] 1.5 HLSCM area-preservation + instance-form tests (mirror 1.2/1.4),
  verifying rescale is applied once to the final multi-level result.
- [ ] Confirm Red: new tests fail on unknown symbols (`RescaleMode`,
  `set_rescale_mode`) with no regressions in existing assertions.

## Phase 2: Core rescale helper (`detail/LSCMSystem.hpp`)
- [ ] 2.1 Define `enum class RescaleMode { Off, LowStretchReference, GlobalAreaMin };`.
- [ ] 2.2 `SnapshotFaceAreas(mesh)` — per-face 3D area from current `pos`,
  captured **before** any UV writeback.
- [ ] 2.3 `RescaleUVs(mesh, mode, originalAreas)` — compute per-face UV areas
  from the written-back UVs, derive the factor per `mode` (area-weighted median
  ratio for `GlobalAreaMin`; least-distortion face anchor for
  `LowStretchReference`), and scale every vertex `x`/`y` about the origin. No-op
  for `Off`.
- [ ] 2.4 Verify Phase 1 helper unit tests (1.1) go Green.

## Phase 3: AngleBasedLSCM integration
- [ ] 3.1 Add `using RescaleMode = detail::lscm::RescaleMode;` public alias.
- [ ] 3.2 Add `set_rescale_mode(RescaleMode)` + `RescaleMode mode_ = Off` member;
  wire into `compute()` dispatch.
- [ ] 3.3 Add `RescaleMode mode = RescaleMode::Off` trailing param to the static
  `Compute(mesh, ...)` and `Compute(mesh, pins, ...)` overloads; thread through
  `ComputeImpl`.
- [ ] 3.4 In `ComputeImpl`: snapshot original face areas before the UV writeback,
  then call `RescaleUVs` after the writeback.
- [ ] 3.5 Doxygen: document each mode, the origin-scaling pin caveat, and the
  3.0 default-change note. Verify ABLSCM tests (1.2–1.4) Green.

## Phase 4: HierarchicalLSCM integration
- [ ] 4.1 Mirror the public alias, setter, member, and static overload params.
- [ ] 4.2 Apply `RescaleUVs` once after the final top-level UV writeback (not per
  hierarchy level); snapshot original areas from the full-resolution mesh.
- [ ] 4.3 Single-level fallback path inherits the ABLSCM behavior; ensure no
  double-rescale when it delegates to `AngleBasedLSCM::Compute`.
- [ ] 4.4 Doxygen parity. Verify HLSCM tests (1.5) Green.

## Phase 5: Verify
- [ ] 5.1 Full `ctest` — all binaries/assertions pass, including unchanged
  explicit-pin and deprecated-shim tests.
- [ ] 5.2 Examples build and run (`examples/`), including the flattening
  benchmark.
- [ ] 5.3 `git clang-format` on changed files.
- [ ] 5.4 Regenerate the amalgamated header
  (`python3 thirdparty/amalgamate/amalgamate.py -c single_include.json -s .`)
  and confirm it builds.

## Phase 6: Conductor & GitHub
- [ ] 6.1 Open PR against #98 (Development-linked); description summarizes the
  opt-in rescale, the three modes, and the planned 3.0 default flip
  (cross-reference #96).
- [ ] 6.2 Move B8 to Archived in `conductor/tracks.md` once the PR lands.

---
_Generated by Conductor._
